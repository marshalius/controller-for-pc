<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ControllerForPC.Pages.Views.ServerView">
    <Border Margin="10">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="20" />
        </Border.StrokeShape>
        <Grid RowDefinitions="*, *" ColumnDefinitions="4*, *" Padding="10" BackgroundColor="#222">
            <Label x:Name="NameLabel" Grid.Row="0" Grid.Column="0" Text="" TextColor="White" />
            <Label x:Name="AddressLabel" Grid.Row="1" Grid.Column="0" Text="" TextColor="White" />
            <Button x:Name="ConnectButton" Grid.RowSpan="2" Grid.Row="0" Grid.Column="1" CornerRadius="20" Text="Bağlan" BackgroundColor="Green" BorderColor="DarkGreen" Clicked="ConnectButton_Clicked" />
        </Grid>
    </Border>
    
</ContentView>
